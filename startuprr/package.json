{
  "name": "startuprr",
  "private": true,
  "scripts": {
    "sass-watch": "node-sass --watch --recursive --output-style compressed --output . --source-map true --source-map-contents .",
    "sass-compile": "node-sass --recursive --output-style compressed --output . --source-map true --source-map-contents .",
    "livereload": "livereload css",
    "start": "run-p sass-watch livereload"
  },
  "devDependencies": {
    "livereload": "^0.9.3",
    "node-sass": "^6.0.1",
    "npm-run-all": "^4.1.5"
  }
}
