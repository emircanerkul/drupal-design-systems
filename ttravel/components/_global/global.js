/**
 * @file
 * Web behaviors.
 */
(function (Drupal) {

    'use strict';

    Drupal.behaviors.webFooter = {
        attach: function (context, settings) {
            console.log('It works!');
        }
    };

}(Drupal));
